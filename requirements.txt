# Pinned for reproducible installs (Phase 4.1). Update periodically; run pytest and pip-audit after bumps.
requests==2.32.5
python-dotenv==1.2.1

# Web app (Step 1+)
fastapi==0.133.1
slowapi==0.1.9
uvicorn[standard]==0.41.0
jinja2==3.1.6
python-multipart==0.0.22

# Tests
pytest==9.0.2
